{% extends "base.html" %}
{% block content %}

<script src="{{ url_for('static', filename='js/milestone_edit.js') }}"></script>

<div class="w3-card-4" style="max-width:50%; margin:auto">

  <header class="w3-container w3-center w3-grey">
    <h1>Edit Milestone Progress</h1>
  </header>

  <div class="w3-container w3-white">
    <form class="w3-container" method="post" action={{ url_for('milestone_edit') }}>
      <select name="editID" class="w3-block w3-section">
        {% for x in milestones %}
        <option value="{{ x.id }}"> {{ x.milestone_id }} {{ x.short_title }}</option>
        {% endfor %}
      </select>

      {{ form.hidden_tag() }}
      <dl>
        <dt>{{ form.progress.label }}:
        <dt>{{ form.progress(rows=5,cols=40) }}
      </dl>
      <br>

      <button class="button" type="submit" id="saveEdit">Save</button>
    </form>
    <br/>

  </div>
</div>

{% endblock %}
